[package]
name = "graph-simulation"
version = "0.1.2"
edition = "2021"
license = "MIT"
description = "A library for simulation algorithm"


[dependencies]
graph-base = "0.1.2"


[dev-dependencies]
criterion = "0.5"

# [[bench]]
# name = "simulation"
# harness = false  # 禁用默认测试框架
